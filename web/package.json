{
  "name": "max9860-evkit-web",
  "private": true,
  "type": "module",
  "description": "Browser host software for the MAX9860 EV kit, over Web Serial. No dependencies and no build step; this file exists so node --test reads the .js sources as ES modules.",
  "scripts": {
    "test": "node --test test/",
    "serve": "python serve.py"
  }
}
